import ("./Header.css")

export default function Header(){
    return <div className={"headerLayout"}>
        <p className={"siteTitle"}>CONVERT</p>
        <p className={"subTitle"}>by HUFTOOLS</p>
    </div>
}